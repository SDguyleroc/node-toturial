setInterval(() => {
    console.log('I will run evry 1 second');
}, 1000)

console.log('I will run first but only once');
 // the process stays alive unless we kill it
 // COntrol-C or unexpected error